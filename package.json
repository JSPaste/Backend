{
	"private": true,
	"name": "jspaste-backend",
	"license": "EUPL-1.2-or-later",
	"module": "src/index.ts",
	"type": "module",
	"scripts": {
		"//": "TODO: Correctly build for production",
		"build": "bun build --entrypoints ./src/index.ts --outdir ./dist --target bun",
		"start": "bun run build && NODE_ENV=production bun ./dist/index.js",
		"dev": "NODE_ENV=development bun --hot ./src/index.ts",
		"lint": "bun prettier --write ."
	},
	"dependencies": {
		"elysia": "^0.8.8",
		"@elysiajs/cors": "^0.8.0",
		"@elysiajs/swagger": "^0.8.3",
		"zlib": "^1.0.5"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"prettier": "^3.1.1"
	},
	"peerDependencies": {
		"typescript": "~5.3.0"
	}
}
